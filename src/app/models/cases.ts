import { CountryCode } from './countrycode';

export class Cases {
    countryregion: string;
    confirmed: string;
    deaths: number;
    recovered: number;
    countrycode: CountryCode;
    lastupdate: string;
  }
